
# How to run

## Backend

From the repo root:

```bash
pip install -r backend/requirements.txt
```

Then:

```bash
python -m backend.run
```

## Frontend (Vite)

From the repo root:

```bash
cd frontend-vite
npm run dev
```

